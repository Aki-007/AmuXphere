// import { Canvas } from '@react-three/fiber';
import ClassroomModel from "./ClassroomModel" // Import your 3D model component

const ClassroomScene = ({ room }) => {
  return <ClassroomModel room={room} />
}

export default ClassroomScene
